imports
    FileSystem as fs
features
    Server { abstract }
        mandatory
            fs.FileSystem
            OperatingSystem { abstract }
                alternative
                    Windows
                    macOS
                    Debian
        optional
            Logging {default , log_level "warn"}
constraints
    Windows => NTFS
    macOS => APFS
